---
description: Rules for UI component development
globs: ["components/ui/**/*"]
---

# UI Components

## Storybook Verification

When creating or modifying UI components:

1. Create a Storybook story file (e.g., `button.stories.tsx`)
2. Run `pnpm storybook` and visually verify the component renders correctly
3. Test all variants and states in Storybook
